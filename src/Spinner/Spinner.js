import React from 'react';

const Spinner = () => {
    return (
        <div>
            <button className="btn text-center btn-ghost loading "></button>
        </div>
    );
};

export default Spinner;